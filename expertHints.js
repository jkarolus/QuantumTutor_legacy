// data.js
const savedData = {
  "I.1.1": ["numpy functions you might need: np.abs() and np.sqrt()","Return the sum of the normalized coefficients multiplied with the basis states"],
  "I.1.2": ["numpy functions you might need: np.conj() and np.dot()","Remember to only apply the conjugate to the first state of the inner product, not the second."],
  "I.1.3": ["numpy functions you might need: np.random.choice([measurement outcomes], p = [corresponding probabilities], size = number of measurements)","make a list of all the probabilities, then return the result of the function of hint 1!"],
  "I.1.4": ["The only numpy function you may need for this is np.dot","U is already defined and can also be used inside the apply_u function!"],
};
